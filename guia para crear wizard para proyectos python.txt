https://www.youtube.com/watch?v=p3tSLatmGvU&t=1215s

crear wizard

instalar anaconda

abrir anaconda prompt

escribir dentro de la consola

conda create --name NOMBREDESEADO python=numeroversionactualizada-> 3.11.4

cuando cargue pregunta si quiere continuar con el proceso presione la tecla y

escriba

conda activate NOMBREDESEADOASIGNADO

escriba

cd RUTADONDETIENELOSARCHIVOSFUENTEDELAAPLICACION

cree dentro de esta carpeta un archivo.txt con todas las dependencias y librerias que requiere su aplicacion,que tenga el siguiente formato

NOMBRELIBRERIA==numerodeversion-> ejemplo adafruit-ampy==1.1.0


escriba

pip install -r NOMBREARCHIVOTXT con las dependencias a instalar

escriba

pip install pyinstaller

pyinstaller ARCHIVOPROYECTOPYTHON.py


Copiar dentro del archivo fuente el siguiente metodo


import sys
import os
def resource_path(relative_path):
    """ Get absolute path to resource, works for dev and for PyInstaller """
    try:
        # PyInstaller creates a temp folder and stores path in _MEIPASS
        base_path = sys._MEIPASS2
    except Exception:
        base_path = os.path.abspath(".")

    return os.path.join(base_path, relative_path)


luego en la consola de anaconda escriba

pyinstaller --name NOMBREDESEADOPARALAAPLICACION --onefile --windowed --icon=NOMBREDELICONOCREADO.ico NOMBREDELARCHIVOPROYECTO.py

descargar INNO desde la pagina oficial

https://jrsoftware.org/isinfo.php

ya con INNO instalado ejecutelo y seleccione la opcion create a new script file using the Script Wizard

seleccione el nombre de la aplicacion
version
empresa propietaria
pagina web
en la ventana aplication files:

ubique el archivo ejecutable creado con annaconda o pyinstaller
agrege los archivos necesarios para que su aplicacion pueda ser ejecutada

desactive la casilla associate a file type to the main executable
cree el archivo de licencia conb la informacion que piense es importante resaltar para la distribucion de su aplicacion

password

1Ng3n13r1410th1x

cambe en el archjivo final en los folders las rutas completas agregando despues de {app}/nombrefolder
